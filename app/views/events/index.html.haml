.col-md-6.col-md-offset-3
  %h1 Lista de eventos  #{link_to 'Novo evento', new_event_path, class: 'btn btn-default btn-xs'}
  %table.table.table-condensed
    %thead
      %tr
        %th Título
        %th Resumo
        %th Horário
        %th Data
        %th Local
        %th Endereço
        %th Apresentador
        %th Responsável
        %th Realização
        %th URL
        %th Tipo
        %th{:colspan => "3"}
    %tbody
      - @events.each do |event|
        %tr
          %td= event.title
          %td= event.summary
          %td= event.time
          %td= event.date
          %td= event.place
          %td= event.address
          %td= event.speaker
          %td= event.responsible
          %td= event.performance
          %td= event.url
          %td= event.event_type_id
          %td= link_to 'Mostrar', event
          %td= link_to 'Editar', edit_event_path(user)
          %td= link_to 'Apagar', event, method: :delete, data: { confirm: 'Tem certeza?' }